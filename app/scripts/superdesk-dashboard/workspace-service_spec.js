define(['./workspace-service'], function(WorkspaceService) {
    'use strict';

    describe('workspace service', function() {

        it('is defined', function() {
            expect(WorkspaceService).not.toBe(undefined);
        });

    });

});
